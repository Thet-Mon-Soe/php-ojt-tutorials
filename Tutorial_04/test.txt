// $uniArr = array_unique($arr1);
                // for ($m=0; $m < count($uniArr); $m++) { 
                //     if($uniArr[$m] == $arr2[$i]){
                //         unset($uniArr[$i]);
                //     }
                // }
                // unset($arr1[$i]);
                // for ($m = 0;$m < count($arr1);$m++) { 
                //     if($arr1[$m] == $arr2[$i]){
                //         unset($arr1[$m]);
                //         continue;
                //         echo "hi <br>";
                //     }
                // }



                // $arr3=array();
    // $new = array();
    // $result = array();
    // for ($i=0; $i < count($arr1); $i++) { 
    //     array_push($arr3,$arr1[$i]);
    // }
    // for ($i=0; $i < count($arr2); $i++) { 
    //     array_push($arr3,$arr2[$i]);
    // }
    // // $uniArr3 = array_unique($arr3);
    
    // for ($k=0; $k < count($arr2); $k++) { 
    //     if(in_array($arr2[$k],$arr3)){
    //         unset($arr3[$k]);
    //     }
    // }
    // $uniArr;